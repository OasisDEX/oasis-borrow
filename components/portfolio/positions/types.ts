export enum PortfolioProductType {
  borrow = 'borrow',
  multiply = 'multiply',
  earn = 'earn',
  migrate = 'migrate',
}

export enum PortfolioSortingType {
  netValueAscending = 'net-value-ascending',
  netValueDescending = 'net-value-descending',
}
