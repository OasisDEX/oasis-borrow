describe('BatchManager', () => {
  it('should batch call into single request', () => {})

  it('should exclude cache hits', () => {})

  it('should cache responses that are not cache hits', () => {})
})
