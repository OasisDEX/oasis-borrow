query Triggers($wallet: Bytes!, $dpm: Bytes!) {
  triggers(where: { owner: $wallet, account: $dpm, triggerType_in: [111, 112, 117, 118, 119, 120] }) {
    account
    owner
    id
    protocol
    triggerData
    triggerType
    addedTimestamp
    decodedData
    decodedDataNames
  }
}
