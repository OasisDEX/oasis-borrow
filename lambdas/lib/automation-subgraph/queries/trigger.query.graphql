query TriggerByType($dpm: Bytes!, $triggerType: BigInt!) {
  triggers(where: { account: $dpm, triggerType: $triggerType  }) {
    account
    owner
    id
    protocol
    triggerType
    triggerData
  }
}
