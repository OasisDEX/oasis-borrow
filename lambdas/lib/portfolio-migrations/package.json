{
  "name": "portfolio-migrations",
  "version": "0.0.1",
  "scripts": {
    "tsc": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "docker:build": "tar chzf files.tar.gz Dockerfile node_modules dist && docker build --platform linux/amd64 -t portfolio-migrations - < files.tar.gz",
    "docker:run": "docker run --rm -p 9000:8080 portfolio-migrations",
    "dev:rerun": "npm docker build && npm docker run",
    "invoke": "curl http://localhost:9000/2015-03-31/functions/function/invocations -d '{}'",
    "aws:deploy": "deploy zip file"
  },
  "author": "Piter",
  "license": "UNLICENSED",
  "devDependencies": {
  },
  "dependencies": {
    "shared": "file:../shared"
  }
}
